/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UpdateShiftsListSRService } from './updateShiftsListSR.service';

describe('Service: UpdateShiftsListSR', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UpdateShiftsListSRService]
    });
  });

  it('should ...', inject([UpdateShiftsListSRService], (service: UpdateShiftsListSRService) => {
    expect(service).toBeTruthy();
  }));
});
